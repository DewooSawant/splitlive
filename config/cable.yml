development:
  adapter: async

test:
  adapter: test

production:
  adapter: <%= ENV["REDIS_URL"].present? ? "redis" : "async" %>
  url: <%= ENV.fetch("REDIS_URL", "") %>
  channel_prefix: splitlive_production
